<div class="row clearfix">
	<div class="col-md-12">
		<div class="col-md-8 column">
			<h3>
				Activité de {% if useracc is iterable %}vos commerciaux{% else %} {{ useracc }}{% endif %}
			</h3>
		</div>
		<div class="col-md-4 column">
			<form role="form" action="{{ path('bacloocrm_listusercr') }}" method="post" {{form_enctype(form) }}>
				<table class="pull-right">
					<tr>
						<td style="padding-top:10px">
							<div class="form-group">
								{{ form_widget(form.username, { 'attr' : { 'class' : 'form-control' } })  }}
							</div>
						</td>
						<td style="padding-top:10px">
							<input type="submit" name="submit" id="submit" value="Valider" class="btn btn-info">   <a type="button" class="btn btn-custom_gris_clair" href="{{ path('bacloocrm_home', {'user' : 'societe'}) }}"><b>Tous</b></span></a>					 
							 {{ form_row(form._token) }}
						</td>
					</tr>
				</table>
			</form>
		</div>
	</div>
</div>